# Icons

The interface uses a small Lucide-style outline icon set rendered as PNG files
for tkinter compatibility.

Lucide is an open-source icon family licensed under the ISC License:
https://lucide.dev

Only local PNG assets are loaded by the app at runtime, so the desktop build
does not need network access.
